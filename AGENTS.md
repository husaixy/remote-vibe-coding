- # Remote Vibe Coding 开发规则

  ## 1. 项目说明

  - 正式产品名称：**Remote Vibe Coding**
  - 中文说明名：**遥控语音编程**
  - 项目目标：在 Windows 上将小米蓝牙遥控器 2 / 2 Pro（RC001 / RC003）作为 Codex 的便携按键与语音输入面板。
  - 新功能应围绕遥控器操作、按键映射、语音输入、配置和状态恢复展开。
  - 不依赖第三方应用的私有数据、内部数据库或私有协议实现核心功能。

  ## 2. 开发原则

  以正常、快速的软件开发流程为主：

  1. 理解需求和现有实现。
  2. 修改相关代码。
  3. 对修改部分进行必要的简单验证。
  4. 确认没有明显问题后提交。
  5. 功能完成后更新 `CHANGELOG.md`。

  开发阶段优先保证功能正确和代码清晰，不为了形式上的覆盖率或验证数量增加额外工作。

  小范围修改不需要运行全量测试、完整构建或安装包验证。

  除非修改本身需要，否则不要顺带重构无关代码。

  ## 3. Git 规则

  - 本仓库是独立 Git 工作库。
  - 保留 `origin` 指向 `miaomiaozii/windows-remote-mic-app` 作为直接上游；个人仓库使用单独 remote。
  - 修改前简单检查 `git status`，避免覆盖已有未提交工作。
  - 不改写上游 Git 历史。
  - 不提交与当前任务无关的修改。
  - 未经用户明确要求，不执行 `push`、发布、rebase、强制推送或历史改写。

  提交按功能划分，保持目的明确即可。

  提交信息使用常规前缀：

  - `feat:` 新功能
  - `fix:` Bug 修复
  - `docs:` 文档
  - `test:` 测试
  - `refactor:` 重构
  - `chore:` 工程或维护工作

  一个完整功能可以作为一个提交，不需要为了细小代码变化拆分大量提交。

  ## 4. 日常验证

  开发阶段只进行与当前修改直接相关的必要验证。

  根据实际修改选择以下一种或几种即可：

  - 检查修改代码是否存在明显语法或类型错误。
  - 运行与修改功能直接相关的测试。
  - 对功能进行一次简单人工冒烟测试。
  - 必要时启动应用确认功能可以正常工作。

  不要默认执行：

  - 全量单元测试
  - 全量代码扫描
  - 完整 PyInstaller 构建
  - Inno Setup 安装包构建
  - Windows 候选版构建
  - 与当前功能无关的历史测试

  只有以下情况才需要扩大验证范围：

  - 用户明确要求。
  - 修改安装器或打包流程。
  - 修改重要公共接口。
  - 修改容易造成资源泄漏、输入状态异常或崩溃的核心逻辑。
  - 准备候选版或正式发布。

  如果需要全量测试：

  ```powershell
  cd apps/windows/rc003
  python -m unittest discover -s tests -t . -p 'test_*.py' -v
  ```

  如果需要候选版构建：

  ```powershell
  apps/windows/rc003/build/build-candidate.ps1
  ```

  测试或构建出现与当前修改明显无关的历史/环境问题时，记录问题即可，不需要为了完成当前功能继续扩大排查范围。

  ## 5. 测试原则

  测试用于防止真实行为回归，而不是审核代码结构。

  新增功能或修复 Bug 时：

  - 有明确且容易自动验证的重要行为，可以补充测试。
  - 简单 UI 调整、文档、配置说明和内部代码整理通常不需要新增测试。
  - 不为了测试文件内容、函数名称、代码结构或文档措辞编写契约式测试。
  - 不追求为了增加测试数量而增加测试。

  真实 RC001、RC003、Codex、输入法和蓝牙行为仍以实际设备验证为准。

  ## 6. Windows 客户端

  Windows 客户端位于：

  ```text
  apps/windows/rc003
  ```

  保持 Qt/QML UI 与 Python 平台逻辑基本分离，避免把大量平台实现直接写入 UI。

  现阶段为了兼容已有安装，继续保留：

  - `RemoteMicRC003.exe`
  - `RemoteMic` 配置目录
  - 现有互斥锁名称
  - `ovb_rc003` Python 包名

  用户可见的新名称统一使用 **Remote Vibe Coding**。

  ## 7. 遥控器与语音键

  语音键保持简单明确的生命周期：

  ```text
  按下 → 开始
  释放 → 结束
  ```

  不要为语音键增加双击等待或不必要的长按判断。

  需要注意：

  - 快速按下和释放必须正常结束。
  - 连续语音会话不能残留上一轮状态。
  - 遥控器断连时应释放相关状态。
  - 应用异常退出或功能停止时尽量清理按键和音频状态。

  遥控器原始语音键可能表现为 `F5`。

  任何针对 `F5` 的特殊处理都必须尽可能限定到目标遥控器，不能为了支持遥控器而全局屏蔽普通键盘的 `F5`。

  基础语音功能不依赖：

  - 管理员权限
  - Frida 注入
  - 虚拟 HID 驱动

  如果某些特殊修复确实需要管理员权限，应作为用户主动启动的独立 Helper，而不是让主程序默认提权。

  ## 8. 版本与 CHANGELOG

  版本遵循 SemVer。

  普通开发过程中：

  - 功能和修复记录到 `apps/windows/rc003/CHANGELOG.md` 的 `Unreleased`。
  - 不需要每完成一个功能就修改版本号。
  - 不需要日常创建 tag。

  只有准备候选版或正式发布时才提升版本。

  发布时检查以下版本保持一致：

  ```text
  apps/windows/rc003/src/ovb_rc003/__init__.py
  apps/windows/rc003/pyproject.toml
  apps/windows/rc003/installer/RemoteMicRC003Setup.iss
  ```

  候选版 tag：

  ```text
  v<版本>-windows-rc003-candidate.<序号>
  ```

  正式版 tag：

  ```text
  v<版本>-windows
  ```

  ## 9. 许可证

  项目继续使用 `GPL-3.0-only`。

  保留：

  ```text
  LICENSE.md
  COPYRIGHT.md
  THIRD_PARTY_NOTICES.md
  apps/windows/rc003/ATTRIBUTION.md
  ```

  如果直接复制或实质改编其他项目的代码，应在归属说明中记录来源仓库、对应提交和相关模块。

  ## 10. 不应提交的内容

  不要提交：

  - `.venv/`
  - `node_modules/`
  - `dist/`
  - `target/`
  - 临时日志
  - 调试输出
  - 原始诊断记录
  - 真实设备唯一标识
  - 用户语音数据
  - 第三方驱动安装包
  - 临时构建产物

  ## 11. 默认工作方式

  没有额外要求时，按照以下方式处理开发任务：

  **阅读相关代码 → 实现功能 → 简单检查 → 必要的最小测试 → 更新 CHANGELOG → 检查 diff → 提交。**

  不要因为小范围修改自动执行全量测试或完整 Windows 构建。

  不要为了满足流程而创建没有实际价值的测试。

  不要擅自扩大任务范围。

  如果当前功能可以通过简单验证确认工作正常，就继续完成任务，不需要追加额外验证流程。

  发布阶段再执行完整测试、候选构建、安装验证和版本检查。
